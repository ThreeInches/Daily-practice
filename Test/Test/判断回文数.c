#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/*int main17() {
	int n;
	int i;
	int tmp, sum = 0;
	printf("请输入一个数字：\n");
	scanf("%d", &n);
	for (i = n; i; i /= 10) {
		tmp = i % 10;
		sum = sum * 10 + tmp;
	}
	if (sum == n) {
		printf("%d是回文数\n", sum);
	}
	else {
		printf("%d不是回文数\n", sum);
	}
	system("pause");
	return 0;
}*/